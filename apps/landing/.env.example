# NEXT_PUBLIC_SITE_URL 是唯一需要配置的项：canonical、Open Graph、sitemap.xml、
# robots.txt 与 llms.txt 全部由它生成。未设置时会回退到 RFC 2606 保留域名
# https://geok.example（永远不会指向真实站点，但会让上述文件全部失效）。
# 这些是 NEXT_PUBLIC_ 前缀，在构建时内联，修改后需要重新构建。
NEXT_PUBLIC_SITE_URL=https://your-domain.example

# 可选：文档站地址。/docs 路由会跳转到这里，llms.txt 也会引用它。
# NEXT_PUBLIC_DOCS_URL=https://docs.your-domain.example

# 可选：GitHub 仓库地址。默认指向 GEOK 仓库。
# NEXT_PUBLIC_GITHUB_REPO_URL=https://github.com/evilstar9527/geok
