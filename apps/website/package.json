{
	"name": "@oneglanse/website",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "python3 -m http.server 3100 --bind 127.0.0.1 --directory site",
		"build": "node scripts/build-embed.mjs",
		"build:standalone": "node scripts/build-embed.mjs --standalone",
		"test": "node scripts/build-embed.mjs && node --test test/*.test.mjs"
	},
	"dependencies": {
		"cheerio": "^1.1.2"
	}
}
